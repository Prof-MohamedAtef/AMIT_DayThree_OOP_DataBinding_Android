<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mo.atef.amit.daythree.daythree">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MaterialCoBridgeTheme">
        <activity
            android:name=".views.activities.AppBarActivty"
            android:exported="false" />
        <activity
            android:name=".views.activities.BottomNavigation"
            android:exported="true"></activity>
        <activity
            android:name=".views.activities.SearchActivity"
            android:exported="true" />
        <activity
            android:name=".views.activities.AddToBasketReadyAppBar"
            android:exported="true" />
        <activity
            android:name=".views.activities.DrawerUseNotAllowed"
            android:exported="true" />
        <activity
            android:name=".views.activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>